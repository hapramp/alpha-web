const initialState = {};
/*
{
	fullPermlink: {
		error: null,
		loading: false,
		replies: {
			replyFullPermlink: {...},
			...
		}
	},
	...
}
*/

export const repliesReducer = (state = initialState, action) => {
	return state;
}
